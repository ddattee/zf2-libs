cache:
  paths:
  - vendor/
  
before_script:
- bash ci/docker_install.sh > /dev/null
- composer install --dev

after_script:
- bash ci/push_github.sh

phpunit:php5.6:
  image: php:5.6
  script:
  - vendor/bin/phpunit --configuration phpunit.xml --colors=never --coverage-text
    
phpunit:php7.0:
  image: php:7.0
  script:
  - vendor/bin/phpunit --configuration phpunit.xml --colors=never --coverage-text

phpunit:php7.1:
  image: php:7.1
  script:
  - vendor/bin/phpunit --configuration phpunit.xml --colors=never --coverage-text
